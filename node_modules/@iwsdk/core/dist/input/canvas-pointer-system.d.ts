/**
 * Copyright (c) Meta Platforms, Inc. and affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */
import { Types } from '../ecs/component.js';
declare const CanvasPointerSystem_base: import("elics").SystemConstructor<{
    /** Forward DOM pointer events from the renderer canvas into the Three scene. */
    enabled: {
        type: Types.Boolean;
        default: true;
    };
    /** Continue forwarding pointer events while an immersive XR session is active. */
    activeDuringXR: {
        type: Types.Boolean;
        default: false;
    };
}, {}, import("../index.js").World, import("../ecs/system.js").System<{
    /** Forward DOM pointer events from the renderer canvas into the Three scene. */
    enabled: {
        type: Types.Boolean;
        default: true;
    };
    /** Continue forwarding pointer events while an immersive XR session is active. */
    activeDuringXR: {
        type: Types.Boolean;
        default: false;
    };
}, {}>>;
export declare class CanvasPointerSystem extends CanvasPointerSystem_base {
    private htmlHandler?;
    private readonly canvasPointerRoot;
    private readonly canvasPointerTraversalMarker;
    private readonly canvasPointerDescendants;
    init(): void;
    update(): void;
    private prepareCanvasPointerRoot;
    private pushDescendants;
    private clearCanvasPointerRoot;
    destroy(): void;
}
export {};
//# sourceMappingURL=canvas-pointer-system.d.ts.map
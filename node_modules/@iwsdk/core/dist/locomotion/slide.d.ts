/**
 * Copyright (c) Meta Platforms, Inc. and affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */
import { Types } from '../ecs/component.js';
declare const SlideSystem_base: import("elics").SystemConstructor<{
    /** Locomotor engine shared across locomotion systems. */
    locomotor: {
        type: Types.Object;
        default: undefined;
    };
    /** Action-backed input provider shared across locomotion systems. */
    inputProvider: {
        type: Types.Object;
        default: undefined;
    };
    /** Maximum linear speed in meters/second. */
    maxSpeed: {
        type: Types.Float32;
        default: number;
    };
    /** Comfort vignette strength [0..1]; 0 disables vignetting. */
    comfortAssist: {
        type: Types.Float32;
        default: number;
    };
    /** Whether jumping is enabled. */
    enableJumping: {
        type: Types.Boolean;
        default: true;
    };
}, {}, import("../index.js").World, import("../ecs/system.js").System<{
    /** Locomotor engine shared across locomotion systems. */
    locomotor: {
        type: Types.Object;
        default: undefined;
    };
    /** Action-backed input provider shared across locomotion systems. */
    inputProvider: {
        type: Types.Object;
        default: undefined;
    };
    /** Maximum linear speed in meters/second. */
    maxSpeed: {
        type: Types.Float32;
        default: number;
    };
    /** Comfort vignette strength [0..1]; 0 disables vignetting. */
    comfortAssist: {
        type: Types.Float32;
        default: number;
    };
    /** Whether jumping is enabled. */
    enableJumping: {
        type: Types.Boolean;
        default: true;
    };
}, {}>>;
/**
 * Analog stick sliding locomotion with optional comfort vignette and jump.
 *
 * @remarks
 * - Reads left controller thumbstick for planar movement relative to head yaw.
 * - Applies a dynamic peripheral vignette based on input magnitude scaled by
 *   `comfortAssist` to reduce motion sickness.
 * - Triggers jump when the locomotion jump action is pressed.
 *
 * @category Locomotion
 */
export declare class SlideSystem extends SlideSystem_base {
    private movementVector;
    private movementDirection;
    private input2D;
    private isMoving;
    private vignette;
    private vignetteAlphaTarget;
    private locomotor;
    private inputProvider;
    init(): void;
    destroy(): void;
    update(delta: number): void;
}
export {};
//# sourceMappingURL=slide.d.ts.map
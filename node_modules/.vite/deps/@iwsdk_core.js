import {
  DEFAULT_ANGULAR_DAMPING,
  DEFAULT_DENSITY,
  DEFAULT_FRICTION,
  DEFAULT_GRAVITY_FACTOR,
  DEFAULT_LINEAR_DAMPING,
  DEFAULT_RESTITUTION,
  PhysicsBody,
  PhysicsManipulation,
  PhysicsShape,
  PhysicsShapeType,
  PhysicsState,
  PhysicsSystem
} from "./chunk-GFN3ZJML.js";
import {
  ActionLocomotionInputProvider,
  AnimatedController,
  AnimatedControllerHand,
  AnimatedHand,
  AssetManager,
  AssetType,
  AudioPool,
  AudioSource,
  AudioSystem,
  AxesState,
  BrowserGamepadAxis,
  BrowserGamepadButton,
  CacheManager,
  CameraFacing,
  CameraSource,
  CameraState,
  CameraSystem,
  CameraUtils,
  CanvasPointerSystem,
  ColorSchemeType,
  CursorVisual,
  DEFAULT_PROFILES_PATH,
  DefaultXRAssetLoader,
  DistanceGrabbable,
  DistanceModel,
  DomeGradient,
  DomeTexture,
  EntityCreator,
  EnvironmentRaycastSystem,
  EnvironmentRaycastTarget,
  EnvironmentSystem,
  EnvironmentType,
  FlexBatchedMesh,
  FollowBehavior,
  FollowSystem,
  Follower,
  GLXFImporter,
  GrabPointer,
  GrabSystem,
  Hovered,
  IBLGradient,
  IBLTexture,
  InputActionManager,
  InputActions,
  InputComponent,
  InputManager,
  InputSystem,
  InstanceStealPolicy,
  Interactable,
  InteractorState,
  LevelRoot,
  LevelSystem,
  LevelTag,
  LocomotionEnvironment,
  LocomotionSystem,
  Locomotor,
  MCPRuntime,
  MovementMode,
  MultiPointer,
  NullEntity,
  OneHandGrabbable,
  PanelDocument,
  PanelUI,
  PanelUISystem,
  PlaybackMode,
  PokeInteractable,
  Pressed,
  RayDisplayMode,
  RayInteractable,
  RayPointer,
  RaycastSpace,
  ReferenceSpaceType,
  SceneUnderstandingSystem,
  ScreenSpace,
  ScreenSpaceUISystem,
  SessionMode,
  SlideSystem,
  StatefulBrowserGamepad,
  StatefulButtonAxesDevice,
  StatefulGamepad,
  StatefulKeyboard,
  SyncedEuler,
  SyncedQuaternion,
  SyncedVector3,
  TOUCH_DEFAULTS,
  TeleportSystem,
  TouchPointer,
  Transform,
  TransformSystem,
  TurnSystem,
  TurningMethod,
  TwoHandsGrabbable,
  UIKitDocument,
  Visibility,
  VisibilityState,
  VisibilitySystem,
  World,
  XRAnchor,
  XRControllerVisualAdapter,
  XRCylinderLayer,
  XRHandVisualAdapter,
  XRInputDeviceType,
  XRInputManager,
  XRInputVisualAdapter,
  XRLayerState,
  XRLayerSystem,
  XRMesh,
  XROrigin,
  XRPlane,
  XRQuadLayer,
  buildSessionInit,
  defaultHandLayout,
  dist_exports,
  fetchJsonFile,
  fetchProfile,
  fetchProfileSync,
  fetchProfilesList,
  getRequiredInputProvider,
  initializeWorld,
  launchXR,
  loadInputProfile,
  normalizeCanvasPointerEventsOptions,
  normalizeReferenceSpec,
  outlineMaterial,
  resolveReferenceSpaceType,
  sampleParabolicCurve,
  setWorldPosition,
  setWorldQuaternion,
  stencilMaterial
} from "./chunk-VL46XR5Q.js";
import {
  ACESFilmicToneMapping,
  AddEquation,
  AddOperation,
  AdditiveAnimationBlendMode,
  AdditiveBlending,
  AgXToneMapping,
  AlphaFormat,
  AlwaysCompare,
  AlwaysDepth,
  AlwaysStencilFunc,
  AmbientLight,
  AnimationAction,
  AnimationClip,
  AnimationLoader,
  AnimationMixer,
  AnimationObjectGroup,
  AnimationUtils,
  ArcCurve,
  ArrayCamera,
  ArrowHelper,
  AttachedBindMode,
  Audio,
  AudioAnalyser,
  AudioContext,
  AudioListener,
  AudioLoader,
  AxesHelper,
  BackSide,
  BasicDepthPacking,
  BasicShadowMap,
  BatchedMesh,
  BezierInterpolant,
  Bone,
  BooleanKeyframeTrack,
  Box2,
  Box3,
  Box3Helper,
  BoxGeometry,
  BoxHelper,
  BufferAttribute,
  BufferGeometry,
  BufferGeometryLoader,
  ByteType,
  Cache,
  Camera,
  CameraHelper,
  CanvasTexture,
  CapsuleGeometry,
  CatmullRomCurve3,
  CineonToneMapping,
  CircleGeometry,
  ClampToEdgeWrapping,
  Clock,
  Color,
  ColorKeyframeTrack,
  ColorManagement,
  Compatibility,
  ComponentRegistry,
  CompressedArrayTexture,
  CompressedCubeTexture,
  CompressedTexture,
  CompressedTextureLoader,
  ConeGeometry,
  ConstantAlphaFactor,
  ConstantColorFactor,
  Controls,
  CubeCamera,
  CubeDepthTexture,
  CubeReflectionMapping,
  CubeRefractionMapping,
  CubeTexture,
  CubeTextureLoader,
  CubeUVReflectionMapping,
  CubicBezierCurve,
  CubicBezierCurve3,
  CubicInterpolant,
  CullFaceBack,
  CullFaceFront,
  CullFaceFrontBack,
  CullFaceNone,
  Curve,
  CurvePath,
  CustomBlending,
  CustomToneMapping,
  CylinderGeometry,
  Cylindrical,
  Data3DTexture,
  DataArrayTexture,
  DataTexture,
  DataTextureLoader,
  DataUtils,
  DecrementStencilOp,
  DecrementWrapStencilOp,
  DefaultLoadingManager,
  DepthFormat,
  DepthStencilFormat,
  DepthTexture,
  DetachedBindMode,
  DirectionalLight,
  DirectionalLightHelper,
  DiscreteInterpolant,
  DodecahedronGeometry,
  DoubleSide,
  DstAlphaFactor,
  DstColorFactor,
  DynamicCopyUsage,
  DynamicDrawUsage,
  DynamicReadUsage,
  EdgesGeometry,
  EllipseCurve,
  Entity,
  EqualCompare,
  EqualDepth,
  EqualStencilFunc,
  EquirectangularReflectionMapping,
  EquirectangularRefractionMapping,
  Euler,
  EventDispatcher,
  ExternalTexture,
  ExtrudeGeometry,
  FileLoader,
  Float16BufferAttribute,
  Float32BufferAttribute,
  FloatType,
  Fog,
  FogExp2,
  FramebufferTexture,
  FrontSide,
  Frustum,
  FrustumArray,
  GLBufferAttribute,
  GLSL1,
  GLSL3,
  Grabbed,
  GreaterCompare,
  GreaterDepth,
  GreaterEqualCompare,
  GreaterEqualDepth,
  GreaterEqualStencilFunc,
  GreaterStencilFunc,
  GridHelper,
  Group,
  HTMLTexture,
  HalfFloatType,
  HemisphereLight,
  HemisphereLightHelper,
  IcosahedronGeometry,
  ImageBitmapLoader,
  ImageLoader,
  ImageUtils,
  IncrementStencilOp,
  IncrementWrapStencilOp,
  InstancedBufferAttribute,
  InstancedBufferGeometry,
  InstancedInterleavedBuffer,
  InstancedMesh,
  Int16BufferAttribute,
  Int32BufferAttribute,
  Int8BufferAttribute,
  IntType,
  InterleavedBuffer,
  InterleavedBufferAttribute,
  Interpolant,
  InterpolateBezier,
  InterpolateDiscrete,
  InterpolateLinear,
  InterpolateSmooth,
  InterpolationSamplingMode,
  InterpolationSamplingType,
  InvertStencilOp,
  KeepStencilOp,
  KeyframeTrack,
  LOD,
  LatheGeometry,
  Layers,
  LessCompare,
  LessDepth,
  LessEqualCompare,
  LessEqualDepth,
  LessEqualStencilFunc,
  LessStencilFunc,
  Light,
  LightProbe,
  Line,
  Line3,
  LineBasicMaterial,
  LineCurve,
  LineCurve3,
  LineDashedMaterial,
  LineLoop,
  LineSegments,
  LinearFilter,
  LinearInterpolant,
  LinearMipMapLinearFilter,
  LinearMipMapNearestFilter,
  LinearMipmapLinearFilter,
  LinearMipmapNearestFilter,
  LinearSRGBColorSpace,
  LinearToneMapping,
  LinearTransfer,
  Loader,
  LoaderUtils,
  LoadingManager,
  LoopOnce,
  LoopPingPong,
  LoopRepeat,
  MOUSE,
  Material,
  MaterialBlending,
  MaterialLoader,
  MathUtils,
  Matrix2,
  Matrix3,
  Matrix4,
  MaxEquation,
  Mesh,
  MeshBasicMaterial,
  MeshDepthMaterial,
  MeshDistanceMaterial,
  MeshLambertMaterial,
  MeshMatcapMaterial,
  MeshNormalMaterial,
  MeshPhongMaterial,
  MeshPhysicalMaterial,
  MeshStandardMaterial,
  MeshToonMaterial,
  MinEquation,
  MirroredRepeatWrapping,
  MixOperation,
  MultiplyBlending,
  MultiplyOperation,
  NearestFilter,
  NearestMipMapLinearFilter,
  NearestMipMapNearestFilter,
  NearestMipmapLinearFilter,
  NearestMipmapNearestFilter,
  NeutralToneMapping,
  NeverCompare,
  NeverDepth,
  NeverStencilFunc,
  NoBlending,
  NoColorSpace,
  NoNormalPacking,
  NoToneMapping,
  NormalAnimationBlendMode,
  NormalBlending,
  NormalGAPacking,
  NormalRGPacking,
  NotEqualCompare,
  NotEqualDepth,
  NotEqualStencilFunc,
  NumberKeyframeTrack,
  Object3D,
  ObjectLoader,
  ObjectSpaceNormalMap,
  OctahedronGeometry,
  OneFactor,
  OneMinusConstantAlphaFactor,
  OneMinusConstantColorFactor,
  OneMinusDstAlphaFactor,
  OneMinusDstColorFactor,
  OneMinusSrcAlphaFactor,
  OneMinusSrcColorFactor,
  OrthographicCamera,
  PCFShadowMap,
  PCFSoftShadowMap,
  PMREMGenerator,
  Path,
  PerspectiveCamera,
  Plane,
  PlaneGeometry,
  PlaneHelper,
  PointLight,
  PointLightHelper,
  Points,
  PointsMaterial,
  PolarGridHelper,
  PolyhedronGeometry,
  PositionalAudio,
  PropertyBinding,
  PropertyMixer,
  QuadraticBezierCurve,
  QuadraticBezierCurve3,
  Quaternion,
  QuaternionKeyframeTrack,
  QuaternionLinearInterpolant,
  R11_EAC_Format,
  RED_GREEN_RGTC2_Format,
  RED_RGTC1_Format,
  REVISION,
  RG11_EAC_Format,
  RGBADepthPacking,
  RGBAFormat,
  RGBAIntegerFormat,
  RGBA_ASTC_10x10_Format,
  RGBA_ASTC_10x5_Format,
  RGBA_ASTC_10x6_Format,
  RGBA_ASTC_10x8_Format,
  RGBA_ASTC_12x10_Format,
  RGBA_ASTC_12x12_Format,
  RGBA_ASTC_4x4_Format,
  RGBA_ASTC_5x4_Format,
  RGBA_ASTC_5x5_Format,
  RGBA_ASTC_6x5_Format,
  RGBA_ASTC_6x6_Format,
  RGBA_ASTC_8x5_Format,
  RGBA_ASTC_8x6_Format,
  RGBA_ASTC_8x8_Format,
  RGBA_BPTC_Format,
  RGBA_ETC2_EAC_Format,
  RGBA_PVRTC_2BPPV1_Format,
  RGBA_PVRTC_4BPPV1_Format,
  RGBA_S3TC_DXT1_Format,
  RGBA_S3TC_DXT3_Format,
  RGBA_S3TC_DXT5_Format,
  RGBDepthPacking,
  RGBFormat,
  RGBIntegerFormat,
  RGB_BPTC_SIGNED_Format,
  RGB_BPTC_UNSIGNED_Format,
  RGB_ETC1_Format,
  RGB_ETC2_Format,
  RGB_PVRTC_2BPPV1_Format,
  RGB_PVRTC_4BPPV1_Format,
  RGB_S3TC_DXT1_Format,
  RGDepthPacking,
  RGFormat,
  RGIntegerFormat,
  RawShaderMaterial,
  Ray,
  Raycaster,
  RectAreaLight,
  RedFormat,
  RedIntegerFormat,
  ReinhardToneMapping,
  RenderTarget,
  RenderTarget3D,
  RepeatWrapping,
  ReplaceStencilOp,
  ReverseSubtractEquation,
  RingGeometry,
  SIGNED_R11_EAC_Format,
  SIGNED_RED_GREEN_RGTC2_Format,
  SIGNED_RED_RGTC1_Format,
  SIGNED_RG11_EAC_Format,
  SRGBColorSpace,
  SRGBTransfer,
  Scene,
  ShaderChunk,
  ShaderLib,
  ShaderMaterial,
  ShadowMaterial,
  Shape,
  ShapeGeometry,
  ShapePath,
  ShapeUtils,
  ShortType,
  Skeleton,
  SkeletonHelper,
  SkinnedMesh,
  Source,
  Sphere,
  SphereGeometry,
  Spherical,
  SphericalHarmonics3,
  SplineCurve,
  SpotLight,
  SpotLightHelper,
  Sprite,
  SpriteMaterial,
  SrcAlphaFactor,
  SrcAlphaSaturateFactor,
  SrcColorFactor,
  StaticCopyUsage,
  StaticDrawUsage,
  StaticReadUsage,
  StereoCamera,
  StreamCopyUsage,
  StreamDrawUsage,
  StreamReadUsage,
  StringKeyframeTrack,
  SubtractEquation,
  SubtractiveBlending,
  TOUCH,
  TangentSpaceNormalMap,
  TetrahedronGeometry,
  Texture,
  TextureLoader,
  TextureUtils,
  Timer,
  TimestampQuery,
  TorusGeometry,
  TorusKnotGeometry,
  Triangle,
  TriangleFanDrawMode,
  TriangleStripDrawMode,
  TrianglesDrawMode,
  TubeGeometry,
  Types,
  UVMapping,
  Uint16BufferAttribute,
  Uint32BufferAttribute,
  Uint8BufferAttribute,
  Uint8ClampedBufferAttribute,
  Uniform,
  UniformsGroup,
  UniformsLib,
  UniformsUtils,
  UnsignedByteType,
  UnsignedInt101111Type,
  UnsignedInt248Type,
  UnsignedInt5999Type,
  UnsignedIntType,
  UnsignedShort4444Type,
  UnsignedShort5551Type,
  UnsignedShortType,
  VERSION,
  VSMShadowMap,
  Vector2,
  Vector3,
  Vector4,
  VectorKeyframeTrack,
  VideoFrameTexture,
  VideoTexture,
  WebGL3DRenderTarget,
  WebGLArrayRenderTarget,
  WebGLCoordinateSystem,
  WebGLCubeRenderTarget,
  WebGLRenderTarget,
  WebGLRenderer,
  WebGLUtils,
  WebGPUCoordinateSystem,
  WebXRController,
  WireframeGeometry,
  WrapAroundEnding,
  ZeroCurvatureEnding,
  ZeroFactor,
  ZeroSlopeEnding,
  ZeroStencilOp,
  createCanvasElement,
  createComponent,
  createSystem,
  eq,
  error,
  ge,
  getConsoleFunction,
  gt,
  isin,
  le,
  log,
  lt,
  ne,
  nin,
  setConsoleFunction,
  warn,
  warnOnce
} from "./chunk-TTD5HGSZ.js";
import "./chunk-PZ5AY32C.js";

// node_modules/@iwsdk/core/dist/version.js
var VERSION2 = "0.4.2";

// node_modules/@iwsdk/core/dist/ecs/component-registry.js
var GLXFComponentRegistry = class {
  /** Register an ECS component class under a GLXF component name. */
  static register(glxfComponentName, componentClass, description, mapper) {
    this.registry.set(glxfComponentName, {
      componentClass,
      description,
      mapper
    });
    this.componentToName.set(componentClass, glxfComponentName);
  }
  /** Return the GLXF name for a given component class. */
  static getComponentName(component) {
    return this.componentToName.get(component);
  }
  /** Lookup a registered mapping entry by GLXF name. */
  static getComponent(glxfComponentName) {
    return this.registry.get(glxfComponentName);
  }
  /** Apply mapped components from GLXF metadata to an entity. */
  static applyComponents(entity, world, glxfComponents) {
    Object.entries(glxfComponents).forEach(([componentName, componentData]) => {
      const entry = this.getComponent(componentName);
      if (entry) {
        const componentProps = entry.mapper ? entry.mapper(componentData) : this.defaultMapper(componentData);
        console.log(`Applying component "${componentName}" to entity with props:`, componentProps);
        if (!entry.componentClass.bitmask) {
          world.registerComponent(entry.componentClass);
        }
        entity.addComponent(entry.componentClass, componentProps);
      } else {
        console.warn(`Component "${componentName}" not found in registry. Available components:`, Array.from(this.registry.keys()));
      }
    });
  }
  /** Default mapper converting GLXF `{ key: { value } }` shape to props. */
  static defaultMapper(glxfData) {
    const props = {};
    Object.entries(glxfData).forEach(([key, fieldData]) => {
      if (fieldData && typeof fieldData === "object" && "value" in fieldData) {
        props[key] = fieldData.value;
      }
    });
    return props;
  }
  /** List all registered GLXF component names. */
  static getRegisteredComponents() {
    return Array.from(this.registry.keys());
  }
  /** Clear the registry (useful for tests). */
  static clear() {
    this.registry.clear();
    this.componentToName.clear();
  }
  /** Placeholder to wire built‑in mappings from generated metadata. */
  static setupBuiltinComponents() {
  }
};
GLXFComponentRegistry.registry = /* @__PURE__ */ new Map();
GLXFComponentRegistry.componentToName = /* @__PURE__ */ new Map();

// node_modules/@iwsdk/core/dist/audio/audio-utils.js
var AudioUtils = class {
  /**
   * Play audio on an entity
   * @param entity - Entity with Audio component
   * @param fadeIn - Fade in duration in seconds
   */
  static play(entity, fadeIn = 0) {
    const index = entity.index;
    if (AudioSource.data.src[index] === void 0) {
      console.warn("Entity does not have Audio component");
      return;
    }
    AudioSource.data._playRequested[index] = 1;
    AudioSource.data._fadeIn[index] = fadeIn;
  }
  /**
   * Pause audio on an entity
   * @param entity - Entity with Audio component
   * @param fadeOut - Fade out duration in seconds
   */
  static pause(entity, fadeOut = 0) {
    const index = entity.index;
    if (AudioSource.data.src[index] === void 0) {
      console.warn("Entity does not have Audio component");
      return;
    }
    AudioSource.data._pauseRequested[index] = 1;
    AudioSource.data._fadeOut[index] = fadeOut;
  }
  /**
   * Stop audio on an entity
   * @param entity - Entity with Audio component
   */
  static stop(entity) {
    const index = entity.index;
    if (AudioSource.data.src[index] === void 0) {
      console.warn("Entity does not have Audio component");
      return;
    }
    AudioSource.data._stopRequested[index] = 1;
  }
  /**
   * Check if audio is playing on an entity
   * @param entity - Entity with Audio component
   * @returns True if audio is playing
   */
  static isPlaying(entity) {
    const index = entity.index;
    if (AudioSource.data.src[index] === void 0) {
      return false;
    }
    return AudioSource.data._isPlaying[index] === 1;
  }
  /**
   * Set volume on an entity's audio
   * @param entity - Entity with Audio component
   * @param volume - Volume level (0.0 to 1.0)
   */
  static setVolume(entity, volume) {
    const index = entity.index;
    if (AudioSource.data.src[index] === void 0) {
      console.warn("Entity does not have Audio component");
      return;
    }
    AudioSource.data.volume[index] = Math.max(0, Math.min(1, volume));
  }
  /**
   * Get volume from an entity's audio
   * @param entity - Entity with Audio component
   * @returns Current volume level
   */
  static getVolume(entity) {
    const index = entity.index;
    if (AudioSource.data.src[index] === void 0) {
      return 0;
    }
    return AudioSource.data.volume[index];
  }
  /**
   * Preload audio for an entity
   * @param entity - Entity with Audio component
   * @returns Promise that resolves when audio is loaded
   */
  static async preload(entity) {
    const index = entity.index;
    if (AudioSource.data.src[index] === void 0) {
      throw new Error("Entity does not have Audio component");
    }
    return new Promise((resolve) => {
      const checkLoaded = () => {
        if (AudioSource.data._loaded[index]) {
          resolve();
        } else if (!AudioSource.data._loading[index] && !AudioSource.data._loaded[index]) {
          setTimeout(checkLoaded, 16);
        } else {
          setTimeout(checkLoaded, 16);
        }
      };
      checkLoaded();
    });
  }
  /**
   * Create a one-shot audio entity that auto-removes after playing
   * @param world - World instance
   * @param src - Audio source path
   * @param options - Additional audio options
   * @returns Created entity
   */
  static createOneShot(world, src, options = {}) {
    var _a, _b;
    const entity = world.createEntity();
    entity.addComponent(AudioSource, {
      src,
      volume: (_a = options.volume) !== null && _a !== void 0 ? _a : 1,
      positional: (_b = options.positional) !== null && _b !== void 0 ? _b : false,
      autoplay: true,
      loop: false
    });
    if (options.positional && options.position) ;
    return entity;
  }
};

// node_modules/@iwsdk/core/dist/depth/depth-occludable.js
var OcclusionShadersMode = {
  /** Soft occlusion with 13-tap blur sampling for smooth edges. */
  SoftOcclusion: "SoftOcclusion",
  /** Hard occlusion with a single depth sample per fragment. */
  HardOcclusion: "HardOcclusion",
  /** MinMax soft occlusion with depth preprocessing for edge-aware smooth edges. */
  MinMaxSoftOcclusion: "MinMaxSoftOcclusion"
};
var DepthOccludable = createComponent("DepthOccludable", {
  mode: {
    type: Types.Enum,
    enum: OcclusionShadersMode,
    default: OcclusionShadersMode.SoftOcclusion
  }
}, "Entity that can be occluded by real-world depth");

// node_modules/@iwsdk/core/dist/depth/depth-textures.js
var DepthTextures = class {
  constructor(useFloat32) {
    this.useFloat32 = useFloat32;
  }
  /**
   * Create or recreate the DataArrayTexture for stereo CPU depth.
   * Packs per-view depth data into a single texture array (depth=2)
   * so the shader can select the correct layer via VIEW_ID.
   */
  createDataArrayTexture(width, height) {
    if (this.dataArrayTexture) {
      this.dataArrayTexture.dispose();
    }
    const depth = 2;
    if (this.useFloat32) {
      const pixelsPerLayer = width * height;
      this.combinedArray = new Float32Array(pixelsPerLayer * depth);
      this.dataArrayTexture = new DataArrayTexture(this.combinedArray, width, height, depth);
      this.dataArrayTexture.format = RedFormat;
      this.dataArrayTexture.type = FloatType;
    } else {
      const bytesPerLayer = width * height * 2;
      this.combinedArray = new Uint8Array(bytesPerLayer * depth);
      this.dataArrayTexture = new DataArrayTexture(this.combinedArray, width, height, depth);
      this.dataArrayTexture.format = RGFormat;
      this.dataArrayTexture.type = UnsignedByteType;
    }
  }
  /**
   * Update the depth texture with new CPU depth data.
   * Copies per-view data into the correct layer of the DataArrayTexture.
   * @param depthData - The CPU depth information from WebXR.
   * @param viewId - The view index (0 for left eye, 1 for right eye).
   */
  updateData(depthData, viewId) {
    if (!this.dataArrayTexture || this.dataArrayTexture.image.width !== depthData.width || this.dataArrayTexture.image.height !== depthData.height) {
      this.createDataArrayTexture(depthData.width, depthData.height);
    }
    if (this.useFloat32) {
      const viewData = new Float32Array(depthData.data);
      const layerSize = depthData.width * depthData.height;
      this.combinedArray.set(viewData, viewId * layerSize);
    } else {
      const viewData = new Uint8Array(depthData.data);
      const layerSize = depthData.width * depthData.height * 2;
      this.combinedArray.set(viewData, viewId * layerSize);
    }
    this.dataArrayTexture.addLayerUpdate(viewId);
    this.dataArrayTexture.needsUpdate = true;
  }
  /**
   * Update the depth texture with native GPU texture from WebXR.
   * The native texture is a texture array containing both eyes' depth data.
   * @param depthData - The GPU depth information from WebXR.
   * @param renderer - The WebGL renderer.
   */
  updateNativeTexture(depthData, renderer) {
    if (!this.nativeTexture) {
      this.nativeTexture = new ExternalTexture(depthData.texture);
    } else {
      this.nativeTexture.sourceTexture = depthData.texture;
    }
    const textureProperties = renderer.properties.get(this.nativeTexture);
    textureProperties.__webglTexture = depthData.texture;
    textureProperties.__version = 1;
  }
  /**
   * Get the native GPU depth texture array.
   * Contains both eyes' depth data; the shader uses VIEW_ID to select the layer.
   */
  getNativeTexture() {
    return this.nativeTexture;
  }
  /**
   * Get the CPU depth data as a DataArrayTexture.
   * Contains per-view depth packed into layers; the shader uses VIEW_ID to select the layer.
   */
  getDataArrayTexture() {
    return this.dataArrayTexture;
  }
  /**
   * Dispose of all depth textures.
   */
  dispose() {
    var _a;
    (_a = this.dataArrayTexture) === null || _a === void 0 ? void 0 : _a.dispose();
    this.dataArrayTexture = void 0;
    this.combinedArray = void 0;
    this.nativeTexture = void 0;
  }
};

// node_modules/three/examples/jsm/postprocessing/Pass.js
var _camera = new OrthographicCamera(-1, 1, 1, -1, 0, 1);
var FullscreenTriangleGeometry = class extends BufferGeometry {
  constructor() {
    super();
    this.setAttribute("position", new Float32BufferAttribute([-1, 3, 0, -1, -1, 0, 3, -1, 0], 3));
    this.setAttribute("uv", new Float32BufferAttribute([0, 2, 0, 0, 2, 0], 2));
  }
};
var _geometry = new FullscreenTriangleGeometry();
var FullScreenQuad = class {
  /**
   * Constructs a new full screen quad.
   *
   * @param {?Material} material - The material to render te full screen quad with.
   */
  constructor(material) {
    this._mesh = new Mesh(_geometry, material);
  }
  /**
   * Frees the GPU-related resources allocated by this instance. Call this
   * method whenever the instance is no longer used in your app.
   */
  dispose() {
    this._mesh.geometry.dispose();
  }
  /**
   * Renders the full screen quad.
   *
   * @param {WebGLRenderer} renderer - The renderer.
   */
  render(renderer) {
    renderer.render(this._mesh, _camera);
  }
  /**
   * The quad's material.
   *
   * @type {?Material}
   */
  get material() {
    return this._mesh.material;
  }
  set material(value) {
    this._mesh.material = value;
  }
};

// node_modules/@iwsdk/core/dist/depth/occlusion/preprocessing-shader.glsl.js
var DepthPreprocessingShader = {
  vertexShader: (
    /* glsl */
    `
varying vec2 vUv;

void main() {
	vUv = uv;
	gl_Position = projectionMatrix * modelViewMatrix * vec4(position, 1.0);
}
`
  ),
  fragmentShader: (
    /* glsl */
    `
precision highp float;

uniform sampler2DArray uXRDepthTextureArray;
uniform float uRawValueToMeters;
uniform int uViewId;
uniform vec2 uDepthTextureSize;
uniform bool uIsGPUDepth;
uniform float uDepthNear;

varying vec2 vUv;

float depthToMeters(float textureValue) {
	if (uIsGPUDepth) {
		return uRawValueToMeters * uDepthNear / (1.0 - textureValue);
	}
	return textureValue * uRawValueToMeters;
}

void main() {
	vec2 texelSize = 1.0 / uDepthTextureSize;
	vec2 baseUV = vUv - texelSize * 1.5;

	// Single pass: sample 4×4 neighborhood, convert, and store in 4 vec4s
	vec4 depths[4];
	float rawMin = 1e20;
	float rawMax = 0.0;
	float sum = 0.0;
	int count = 0;

	for (int row = 0; row < 4; row++) {
		for (int col = 0; col < 4; col++) {
			vec2 sampleUV = baseUV + vec2(float(col), float(row)) * texelSize;
			float rawValue = texture(uXRDepthTextureArray, vec3(sampleUV, float(uViewId))).r;
			float meters = depthToMeters(rawValue);
			depths[row][col] = meters;

			if (meters > 0.0) {
				rawMin = min(rawMin, meters);
				rawMax = max(rawMax, meters);
				sum += meters;
				count++;
			}
		}
	}

	if (count == 0) {
		gl_FragColor = vec4(0.0);
		return;
	}

	float avg = sum / float(count);

	// Group samples near the min (within 115%) and near the max (within 85%)
	// to compute stable minAvg and maxAvg — ported from CalculateMinMaxDepth
	float minThreshold = rawMin * 1.15;
	float maxThreshold = rawMax * 0.85;

	// Degenerate case: neighborhood is uniform (within both thresholds)
	if (maxThreshold < rawMin && minThreshold > rawMax) {
		gl_FragColor = vec4(avg, avg, 0.0, 0.0);
		return;
	}

	// Vectorized clustering using stored depths — no second sampling loop
	float minSumDepth = 0.0;
	float minSumCount = 0.0;
	float maxSumDepth = 0.0;
	float maxSumCount = 0.0;

	for (int i = 0; i < 4; i++) {
		vec4 valid = vec4(greaterThan(depths[i], vec4(0.0)));
		vec4 minMask = vec4(lessThanEqual(depths[i], vec4(minThreshold))) * valid;
		vec4 maxMask = vec4(greaterThanEqual(depths[i], vec4(maxThreshold))) * valid;
		minSumDepth += dot(minMask, depths[i]);
		minSumCount += dot(minMask, vec4(1.0));
		maxSumDepth += dot(maxMask, depths[i]);
		maxSumCount += dot(maxMask, vec4(1.0));
	}

	float minAvg = minSumCount > 0.0 ? minSumDepth / minSumCount : rawMin;
	float maxAvg = maxSumCount > 0.0 ? maxSumDepth / maxSumCount : rawMax;

	gl_FragColor = vec4(minAvg, maxAvg, avg - minAvg, maxAvg - minAvg);
}
`
  )
};

// node_modules/@iwsdk/core/dist/depth/occlusion/preprocessing-pass.js
var DepthPreprocessingPass = class {
  constructor() {
    this.material = new ShaderMaterial({
      name: "DepthPreprocessing",
      uniforms: {
        uXRDepthTextureArray: { value: null },
        uRawValueToMeters: { value: 1e-3 },
        uViewId: { value: 0 },
        uDepthTextureSize: { value: new Vector2() },
        uIsGPUDepth: { value: false },
        uDepthNear: { value: 0 }
      },
      vertexShader: DepthPreprocessingShader.vertexShader,
      fragmentShader: DepthPreprocessingShader.fragmentShader
    });
    const rtOptions = { format: RGBAFormat, type: FloatType };
    this.renderTargets = [
      new WebGLRenderTarget(1, 1, rtOptions),
      new WebGLRenderTarget(1, 1, rtOptions)
    ];
    this.fsQuad = new FullScreenQuad(this.material);
  }
  /**
   * Set the depth texture source for preprocessing.
   * @param depthTexture - The XR depth texture array (ExternalTexture or DataArrayTexture).
   * @param rawValueToMeters - Conversion factor from raw depth values to meters.
   * @param isGPUDepth - Whether the depth data is GPU-optimized (inverse depth).
   * @param depthNear - The near plane distance for GPU depth conversion.
   */
  setDepthTexture(depthTexture, rawValueToMeters, isGPUDepth, depthNear) {
    this.material.uniforms.uXRDepthTextureArray.value = depthTexture;
    this.material.uniforms.uRawValueToMeters.value = rawValueToMeters;
    this.material.uniforms.uIsGPUDepth.value = isGPUDepth;
    this.material.uniforms.uDepthNear.value = depthNear;
  }
  /**
   * Render the preprocessing pass.
   * @param renderer - The three.js WebGL renderer.
   * @param depthWidth - Width of the depth texture.
   * @param depthHeight - Height of the depth texture.
   * @param viewId - The view index to render (default 0).
   */
  render(renderer, depthWidth, depthHeight, viewId = 0) {
    const target = this.renderTargets[viewId];
    if (target.width !== depthWidth || target.height !== depthHeight) {
      target.setSize(depthWidth, depthHeight);
    }
    this.material.uniforms.uViewId.value = viewId;
    this.material.uniforms.uDepthTextureSize.value.set(depthWidth, depthHeight);
    const originalRenderTarget = renderer.getRenderTarget();
    const xrEnabled = renderer.xr.enabled;
    renderer.xr.enabled = false;
    renderer.setRenderTarget(target);
    this.fsQuad.render(renderer);
    renderer.setRenderTarget(originalRenderTarget);
    renderer.xr.enabled = xrEnabled;
  }
  /**
   * Get the preprocessed depth texture for a given view.
   * RGBA = (minAvg, maxAvg, avg - minAvg, maxAvg - minAvg) in meters.
   * @param viewId - The view index (0 for left eye, 1 for right eye).
   */
  getTexture(viewId = 0) {
    return this.renderTargets[viewId].texture;
  }
  /**
   * Dispose of all resources.
   */
  dispose() {
    for (const target of this.renderTargets) {
      target.dispose();
    }
    this.material.dispose();
    this.fsQuad.dispose();
  }
};

// node_modules/@iwsdk/core/dist/depth/depth-sensing-system.js
var DepthSensingSystem = class _DepthSensingSystem extends createSystem({
  occludables: { required: [DepthOccludable] }
}, {
  enableOcclusion: { type: Types.Boolean, default: true },
  enableDepthTexture: { type: Types.Boolean, default: true },
  useFloat32: { type: Types.Boolean, default: true },
  blurRadius: { type: Types.Float32, default: 20 }
}) {
  constructor() {
    super(...arguments);
    this.cpuDepthData = [];
    this.gpuDepthData = [];
    this.entityShaderMap = /* @__PURE__ */ new Map();
    this.viewportSize = new Vector2();
    this.minMaxEntityCount = 0;
  }
  /**
   * Get the raw value to meters conversion factor.
   */
  get rawValueToMeters() {
    if (this.cpuDepthData.length) {
      return this.cpuDepthData[0].rawValueToMeters;
    } else if (this.gpuDepthData.length) {
      return this.gpuDepthData[0].rawValueToMeters;
    }
    return 0;
  }
  init() {
    this.xrManager.addEventListener("sessionstart", () => {
      this.updateEnabledFeatures(this.xrManager.getSession());
    });
    this.xrManager.addEventListener("sessionend", () => {
      this.cleanup();
    });
    this.config.enableDepthTexture.subscribe((enabled) => {
      if (enabled && !this.depthTextures) {
        this.initializeDepthTextures();
      }
    });
    this.queries.occludables.subscribe("qualify", (entity) => {
      this.attachOcclusionToEntity(entity);
      if (DepthOccludable.data.mode[entity.index] === OcclusionShadersMode.MinMaxSoftOcclusion) {
        this.minMaxEntityCount++;
      }
    });
    this.queries.occludables.subscribe("disqualify", (entity) => {
      if (DepthOccludable.data.mode[entity.index] === OcclusionShadersMode.MinMaxSoftOcclusion) {
        this.minMaxEntityCount--;
      }
      this.detachOcclusionFromEntity(entity);
    });
  }
  initializeDepthTextures() {
    this.depthTextures = new DepthTextures(this.config.useFloat32.value);
  }
  /**
   * Injects inline depth occlusion shader code into all materials of an entity.
   */
  attachOcclusionToEntity(entity) {
    const object3D = entity.object3D;
    if (!object3D) {
      return;
    }
    const entityUniforms = /* @__PURE__ */ new Set();
    this.entityShaderMap.set(entity, entityUniforms);
    object3D.traverse((child) => {
      var _a;
      if (!(child instanceof Mesh)) {
        return;
      }
      const materials = Array.isArray(child.material) ? child.material : [child.material];
      for (const material of materials) {
        if (!material) {
          continue;
        }
        material.transparent = true;
        const existingCallback = (_a = material.onBeforeCompile) === null || _a === void 0 ? void 0 : _a.bind(material);
        material.onBeforeCompile = (shader, renderer) => {
          if (existingCallback) {
            existingCallback(shader, renderer);
          }
          if (!shader.uniforms.occlusionEnabled) {
            _DepthSensingSystem.addOcclusionToShader(shader);
          }
          material.userData.shader = shader;
          entityUniforms.add(shader.uniforms);
        };
        material.needsUpdate = true;
      }
    });
  }
  detachOcclusionFromEntity(entity) {
    this.entityShaderMap.delete(entity);
  }
  /**
   * Modifies a material's shader in-place to incorporate inline depth-based
   * occlusion. Compares the virtual fragment's view-space depth against the
   * real-world depth from the XR depth texture array.
   * Both GPU-optimized (ExternalTexture) and CPU-optimized (DataArrayTexture)
   * paths use sampler2DArray with VIEW_ID for correct stereo depth sampling.
   * @param shader - The shader object provided by onBeforeCompile.
   */
  static addOcclusionToShader(shader) {
    var _a;
    shader.uniforms.occlusionEnabled = { value: false };
    shader.uniforms.uXRDepthTextureArray = { value: null };
    shader.uniforms.uRawValueToMeters = { value: 1e-3 };
    shader.uniforms.uIsGPUDepth = { value: false };
    shader.uniforms.uDepthNear = { value: 0 };
    shader.uniforms.uViewportSize = { value: new Vector2() };
    shader.uniforms.uOcclusionBlurRadius = { value: 20 };
    shader.uniforms.uOcclusionHardMode = { value: false };
    shader.uniforms.uOcclusionMinMaxMode = { value: false };
    shader.uniforms.uMinMaxTexture0 = { value: null };
    shader.uniforms.uMinMaxTexture1 = { value: null };
    shader.defines = {
      ...(_a = shader.defines) !== null && _a !== void 0 ? _a : {},
      USE_UV: true
    };
    shader.vertexShader = shader.vertexShader.replace("#include <common>", ["varying float vOcclusionViewDepth;", "#include <common>"].join("\n")).replace("#include <fog_vertex>", [
      "#include <fog_vertex>",
      "vec4 occlusion_view_pos = modelViewMatrix * vec4(position, 1.0);",
      "vOcclusionViewDepth = -occlusion_view_pos.z;"
    ].join("\n"));
    shader.fragmentShader = shader.fragmentShader.replace("uniform vec3 diffuse;", [
      "uniform vec3 diffuse;",
      "uniform bool occlusionEnabled;",
      "uniform float uRawValueToMeters;",
      "uniform bool uIsGPUDepth;",
      "uniform float uDepthNear;",
      "uniform vec2 uViewportSize;",
      "uniform float uOcclusionBlurRadius;",
      "uniform bool uOcclusionHardMode;",
      "uniform bool uOcclusionMinMaxMode;",
      "uniform sampler2D uMinMaxTexture0;",
      "uniform sampler2D uMinMaxTexture1;",
      "varying float vOcclusionViewDepth;",
      "",
      "uniform sampler2DArray uXRDepthTextureArray;",
      "",
      "// Fallback for non-multiview sessions",
      "#ifndef VIEW_ID",
      "#define VIEW_ID 0",
      "#endif",
      "",
      "float OcclusionDepthGetMeters(in vec2 uv) {",
      "  float textureValue = texture(uXRDepthTextureArray, vec3(uv.x, uv.y, float(VIEW_ID))).r;",
      "  if (uIsGPUDepth) {",
      "    return uRawValueToMeters * uDepthNear / (1.0 - textureValue);",
      "  }",
      "  return textureValue * uRawValueToMeters;",
      "}",
      "",
      "float OcclusionGetSample(in vec2 depthUV, in vec2 offset) {",
      "  float sampleDepth = OcclusionDepthGetMeters(depthUV + offset);",
      "  return smoothstep(0.0, 0.05, sampleDepth - vOcclusionViewDepth);",
      "}"
    ].join("\n")).replace("vec4 diffuseColor = vec4( diffuse, opacity );", [
      "vec4 diffuseColor = vec4( diffuse, opacity );",
      "if (occlusionEnabled) {",
      "  vec2 screenUV = gl_FragCoord.xy / uViewportSize;",
      "  vec2 depthUV = uIsGPUDepth ? screenUV : vec2(screenUV.x, 1.0 - screenUV.y);",
      "  float occlusion_value;",
      "  if (uOcclusionMinMaxMode) {",
      "    // MinMax soft occlusion — two-cluster edge-aware blending",
      "    vec4 mmData;",
      "    if (uint(VIEW_ID) == 0u) {",
      "      mmData = texture2D(uMinMaxTexture0, depthUV);",
      "    } else {",
      "      mmData = texture2D(uMinMaxTexture1, depthUV);",
      "    }",
      "    float minAvgDepth = mmData.r;",
      "    float maxAvgDepth = mmData.g;",
      "    float midAvgDepth = mmData.r + mmData.b;",
      "    float fadeRange = vOcclusionViewDepth * 0.04;",
      "    float fadeRangeInv = 1.0 / max(fadeRange, 0.001);",
      "    vec3 envDepths = vec3(minAvgDepth, maxAvgDepth, midAvgDepth);",
      "    vec3 occAlphas = clamp((envDepths - vOcclusionViewDepth) * fadeRangeInv, 0.0, 1.0);",
      "    occlusion_value = occAlphas.z;",
      "    float alphaDiff = occAlphas.y - occAlphas.x;",
      "    if (alphaDiff > 0.03) {",
      "      float denom = mmData.a;",
      "      float interp = denom > 0.001 ? mmData.b / denom : 0.5;",
      "      occlusion_value = mix(occAlphas.x, occAlphas.y, smoothstep(0.2, 0.8, interp));",
      "    }",
      "  } else if (uOcclusionHardMode) {",
      "    occlusion_value = OcclusionGetSample(depthUV, vec2(0.0));",
      "  } else {",
      "   vec2 texelSize = uOcclusionBlurRadius / uViewportSize;",
      "   // 13-tap two-ring sampling pattern for smooth occlusion edges",
      "   // Center sample",
      "   occlusion_value = OcclusionGetSample(depthUV, vec2(0.0));",
      "   // Inner ring: 6 samples at 40% radius, 60 degree intervals",
      "   occlusion_value += OcclusionGetSample(depthUV, texelSize * vec2( 0.4,  0.0));",
      "   occlusion_value += OcclusionGetSample(depthUV, texelSize * vec2( 0.2,  0.346));",
      "   occlusion_value += OcclusionGetSample(depthUV, texelSize * vec2(-0.2,  0.346));",
      "   occlusion_value += OcclusionGetSample(depthUV, texelSize * vec2(-0.4,  0.0));",
      "   occlusion_value += OcclusionGetSample(depthUV, texelSize * vec2(-0.2, -0.346));",
      "   occlusion_value += OcclusionGetSample(depthUV, texelSize * vec2( 0.2, -0.346));",
      "   // Outer ring: 6 samples at full radius, offset 30 degrees",
      "   occlusion_value += OcclusionGetSample(depthUV, texelSize * vec2( 0.866,  0.5));",
      "   occlusion_value += OcclusionGetSample(depthUV, texelSize * vec2( 0.0,    1.0));",
      "   occlusion_value += OcclusionGetSample(depthUV, texelSize * vec2(-0.866,  0.5));",
      "   occlusion_value += OcclusionGetSample(depthUV, texelSize * vec2(-0.866, -0.5));",
      "   occlusion_value += OcclusionGetSample(depthUV, texelSize * vec2( 0.0,   -1.0));",
      "   occlusion_value += OcclusionGetSample(depthUV, texelSize * vec2( 0.866, -0.5));",
      "   occlusion_value /= 13.0;",
      "  }",
      "  if (occlusion_value < 0.01) {",
      "      discard;",
      "  }",
      "  diffuseColor.a *= occlusion_value;",
      "}"
    ].join("\n"));
  }
  cleanup() {
    var _a;
    this.depthFeatureEnabled = void 0;
    this.cpuDepthData = [];
    this.gpuDepthData = [];
    (_a = this.preprocessingPass) === null || _a === void 0 ? void 0 : _a.dispose();
    this.preprocessingPass = void 0;
  }
  updateEnabledFeatures(xrSession) {
    if (!xrSession) {
      return;
    }
    const enabledFeatures = xrSession.enabledFeatures;
    this.depthFeatureEnabled = enabledFeatures === null || enabledFeatures === void 0 ? void 0 : enabledFeatures.includes("depth-sensing");
    if (!this.depthFeatureEnabled) {
      console.log("Warning: depth-sensing feature not enabled for WebXR session. Depth sensing features are disabled.");
    }
  }
  update() {
    if (!this.depthFeatureEnabled) {
      return;
    }
    const frame = this.xrFrame;
    if (frame) {
      this.updateLocalDepth(frame);
    }
    if (this.config.enableOcclusion.value) {
      this.runMinMaxPreprocessing();
      this.updateOcclusionUniforms();
    }
  }
  /**
   * Runs the MinMax depth preprocessing pass if any entity uses MinMaxSoftOcclusion.
   * Renders a fullscreen pass per eye that computes min/max/avg depth in a 4×4
   * neighborhood, outputting to per-view 2D render targets.
   */
  get isGPUDepth() {
    const session = this.xrManager.getSession();
    return (session === null || session === void 0 ? void 0 : session.depthUsage) === "gpu-optimized";
  }
  getDepthTextureArray() {
    var _a, _b;
    return this.isGPUDepth ? (_a = this.depthTextures) === null || _a === void 0 ? void 0 : _a.getNativeTexture() : (_b = this.depthTextures) === null || _b === void 0 ? void 0 : _b.getDataArrayTexture();
  }
  runMinMaxPreprocessing() {
    var _a, _b;
    if (this.minMaxEntityCount === 0) {
      return;
    }
    const depthTextureArray = this.getDepthTextureArray();
    if (!depthTextureArray) {
      return;
    }
    const isGPUDepth = this.isGPUDepth;
    const depthNear = (_b = (_a = this.gpuDepthData[0]) === null || _a === void 0 ? void 0 : _a.depthNear) !== null && _b !== void 0 ? _b : 0;
    if (!this.preprocessingPass) {
      this.preprocessingPass = new DepthPreprocessingPass();
    }
    this.preprocessingPass.setDepthTexture(depthTextureArray, this.rawValueToMeters, isGPUDepth, depthNear);
    let depthWidth;
    let depthHeight;
    if (this.cpuDepthData[0]) {
      depthWidth = this.cpuDepthData[0].width;
      depthHeight = this.cpuDepthData[0].height;
    } else if (this.gpuDepthData[0]) {
      depthWidth = this.gpuDepthData[0].width;
      depthHeight = this.gpuDepthData[0].height;
    } else {
      return;
    }
    this.preprocessingPass.render(this.renderer, depthWidth, depthHeight, 0);
    this.preprocessingPass.render(this.renderer, depthWidth, depthHeight, 1);
  }
  /**
   * Updates depth data from the XR frame.
   */
  updateLocalDepth(frame) {
    const session = frame.session;
    const binding = this.renderer.xr.getBinding();
    const xrRefSpace = this.renderer.xr.getReferenceSpace();
    if (xrRefSpace) {
      const pose = frame.getViewerPose(xrRefSpace);
      if (pose) {
        if (session.depthUsage === "gpu-optimized") {
          const view = pose.views[0];
          const depthData = binding.getDepthInformation(view);
          if (depthData) {
            this.updateGPUDepthData(depthData);
          }
        } else {
          for (let viewId = 0; viewId < pose.views.length; ++viewId) {
            const view = pose.views[viewId];
            const depthData = frame.getDepthInformation(view);
            if (!depthData) {
              return;
            }
            this.updateCPUDepthData(depthData, viewId);
          }
        }
      }
    }
  }
  /**
   * Update with CPU-optimized depth data.
   */
  updateCPUDepthData(depthData, viewId = 0) {
    this.cpuDepthData[viewId] = depthData;
    if (this.config.enableDepthTexture.value && this.depthTextures) {
      this.depthTextures.updateData(depthData, viewId);
    }
  }
  /**
   * Update with GPU-optimized depth data.
   */
  updateGPUDepthData(depthData) {
    this.gpuDepthData[0] = depthData;
    if (this.config.enableDepthTexture.value && this.depthTextures) {
      this.depthTextures.updateNativeTexture(depthData, this.renderer);
    }
  }
  /**
   * Updates depth texture uniforms on all occludable materials each frame.
   * Both GPU and CPU paths set the texture array uniform; the shader uses
   * VIEW_ID to select the correct stereo layer.
   */
  updateOcclusionUniforms() {
    var _a, _b;
    const depthTextureArray = this.getDepthTextureArray();
    if (!depthTextureArray) {
      return;
    }
    const isGPUDepth = this.isGPUDepth;
    const depthNear = (_b = (_a = this.gpuDepthData[0]) === null || _a === void 0 ? void 0 : _a.depthNear) !== null && _b !== void 0 ? _b : 0;
    this.renderer.getDrawingBufferSize(this.viewportSize);
    for (const [entity, entityUniforms] of this.entityShaderMap) {
      const isHardMode = DepthOccludable.data.mode[entity.index] === OcclusionShadersMode.HardOcclusion;
      const isMinMaxMode = DepthOccludable.data.mode[entity.index] === OcclusionShadersMode.MinMaxSoftOcclusion;
      for (const uniforms of entityUniforms) {
        uniforms.uXRDepthTextureArray.value = depthTextureArray;
        uniforms.uRawValueToMeters.value = this.rawValueToMeters;
        uniforms.uIsGPUDepth.value = isGPUDepth;
        uniforms.uDepthNear.value = depthNear;
        uniforms.uViewportSize.value.copy(this.viewportSize);
        uniforms.uOcclusionBlurRadius.value = this.config.blurRadius.value;
        uniforms.uOcclusionHardMode.value = isHardMode;
        uniforms.uOcclusionMinMaxMode.value = isMinMaxMode;
        uniforms.occlusionEnabled.value = this.config.enableOcclusion.value;
        if (isMinMaxMode && this.preprocessingPass) {
          uniforms.uMinMaxTexture0.value = this.preprocessingPass.getTexture(0);
          uniforms.uMinMaxTexture1.value = this.preprocessingPass.getTexture(1);
        }
      }
    }
  }
};

// node_modules/@iwsdk/core/dist/index.js
console.log(`                                                 
▄▄▄▄▄▄  ▄▄      ▄▄   ▄▄▄▄    ▄▄▄▄▄     ▄▄   ▄▄▄ 
▀▀██▀▀  ██      ██ ▄█▀▀▀▀█   ██▀▀▀██   ██  ██▀  
  ██    ▀█▄ ██ ▄█▀ ██▄       ██    ██  ██▄██    
  ██     ██ ██ ██   ▀████▄   ██    ██  █████    
  ██     ███▀▀███       ▀██  ██    ██  ██  ██▄  
▄▄██▄▄   ███  ███  █▄▄▄▄▄█▀  ██▄▄▄██   ██   ██▄ 
▀▀▀▀▀▀   ▀▀▀  ▀▀▀   ▀▀▀▀▀    ▀▀▀▀▀     ▀▀    ▀▀                                                
===============================================
Immersive Web SDK v${VERSION2}
Three.js r${REVISION}
EliCS v${VERSION}`);
export {
  ACESFilmicToneMapping,
  ActionLocomotionInputProvider,
  AddEquation,
  AddOperation,
  AdditiveAnimationBlendMode,
  AdditiveBlending,
  AgXToneMapping,
  AlphaFormat,
  AlwaysCompare,
  AlwaysDepth,
  AlwaysStencilFunc,
  AmbientLight,
  AnimatedController,
  AnimatedControllerHand,
  AnimatedHand,
  AnimationAction,
  AnimationClip,
  AnimationLoader,
  AnimationMixer,
  AnimationObjectGroup,
  AnimationUtils,
  ArcCurve,
  ArrayCamera,
  ArrowHelper,
  AssetManager,
  AssetType,
  AttachedBindMode,
  Audio,
  AudioAnalyser,
  AudioContext,
  AudioListener,
  AudioLoader,
  AudioPool,
  AudioSource,
  AudioSystem,
  AudioUtils,
  AxesHelper,
  AxesState,
  BackSide,
  BasicDepthPacking,
  BasicShadowMap,
  BatchedMesh,
  BezierInterpolant,
  Bone,
  BooleanKeyframeTrack,
  Box2,
  Box3,
  Box3Helper,
  BoxGeometry,
  BoxHelper,
  BrowserGamepadAxis,
  BrowserGamepadButton,
  BufferAttribute,
  BufferGeometry,
  BufferGeometryLoader,
  ByteType,
  Cache,
  CacheManager,
  Camera,
  CameraFacing,
  CameraHelper,
  CameraSource,
  CameraState,
  CameraSystem,
  CameraUtils,
  CanvasPointerSystem,
  CanvasTexture,
  CapsuleGeometry,
  CatmullRomCurve3,
  CineonToneMapping,
  CircleGeometry,
  ClampToEdgeWrapping,
  Clock,
  Color,
  ColorKeyframeTrack,
  ColorManagement,
  ColorSchemeType,
  Compatibility,
  ComponentRegistry,
  CompressedArrayTexture,
  CompressedCubeTexture,
  CompressedTexture,
  CompressedTextureLoader,
  ConeGeometry,
  ConstantAlphaFactor,
  ConstantColorFactor,
  Controls,
  CubeCamera,
  CubeDepthTexture,
  CubeReflectionMapping,
  CubeRefractionMapping,
  CubeTexture,
  CubeTextureLoader,
  CubeUVReflectionMapping,
  CubicBezierCurve,
  CubicBezierCurve3,
  CubicInterpolant,
  CullFaceBack,
  CullFaceFront,
  CullFaceFrontBack,
  CullFaceNone,
  CursorVisual,
  Curve,
  CurvePath,
  CustomBlending,
  CustomToneMapping,
  CylinderGeometry,
  Cylindrical,
  DEFAULT_ANGULAR_DAMPING,
  DEFAULT_DENSITY,
  DEFAULT_FRICTION,
  DEFAULT_GRAVITY_FACTOR,
  DEFAULT_LINEAR_DAMPING,
  DEFAULT_PROFILES_PATH,
  DEFAULT_RESTITUTION,
  Data3DTexture,
  DataArrayTexture,
  DataTexture,
  DataTextureLoader,
  DataUtils,
  DecrementStencilOp,
  DecrementWrapStencilOp,
  DefaultLoadingManager,
  DefaultXRAssetLoader,
  DepthFormat,
  DepthOccludable,
  DepthSensingSystem,
  DepthStencilFormat,
  DepthTexture,
  DepthTextures,
  DetachedBindMode,
  DirectionalLight,
  DirectionalLightHelper,
  DiscreteInterpolant,
  DistanceGrabbable,
  DistanceModel,
  DodecahedronGeometry,
  DomeGradient,
  DomeTexture,
  DoubleSide,
  DstAlphaFactor,
  DstColorFactor,
  DynamicCopyUsage,
  DynamicDrawUsage,
  DynamicReadUsage,
  EdgesGeometry,
  EllipseCurve,
  Entity,
  EntityCreator,
  EnvironmentRaycastSystem,
  EnvironmentRaycastTarget,
  EnvironmentSystem,
  EnvironmentType,
  EqualCompare,
  EqualDepth,
  EqualStencilFunc,
  EquirectangularReflectionMapping,
  EquirectangularRefractionMapping,
  Euler,
  EventDispatcher,
  ExternalTexture,
  ExtrudeGeometry,
  FileLoader,
  FlexBatchedMesh,
  Float16BufferAttribute,
  Float32BufferAttribute,
  FloatType,
  Fog,
  FogExp2,
  FollowBehavior,
  FollowSystem,
  Follower,
  FramebufferTexture,
  FrontSide,
  Frustum,
  FrustumArray,
  GLBufferAttribute,
  GLSL1,
  GLSL3,
  GLXFComponentRegistry,
  GLXFImporter,
  GrabPointer,
  GrabSystem,
  Grabbed,
  GreaterCompare,
  GreaterDepth,
  GreaterEqualCompare,
  GreaterEqualDepth,
  GreaterEqualStencilFunc,
  GreaterStencilFunc,
  GridHelper,
  Group,
  HTMLTexture,
  HalfFloatType,
  HemisphereLight,
  HemisphereLightHelper,
  Hovered,
  IBLGradient,
  IBLTexture,
  IcosahedronGeometry,
  ImageBitmapLoader,
  ImageLoader,
  ImageUtils,
  IncrementStencilOp,
  IncrementWrapStencilOp,
  InputActionManager,
  InputActions,
  InputComponent,
  InputManager,
  InputSystem,
  InstanceStealPolicy,
  InstancedBufferAttribute,
  InstancedBufferGeometry,
  InstancedInterleavedBuffer,
  InstancedMesh,
  Int16BufferAttribute,
  Int32BufferAttribute,
  Int8BufferAttribute,
  IntType,
  Interactable,
  InteractorState,
  InterleavedBuffer,
  InterleavedBufferAttribute,
  Interpolant,
  InterpolateBezier,
  InterpolateDiscrete,
  InterpolateLinear,
  InterpolateSmooth,
  InterpolationSamplingMode,
  InterpolationSamplingType,
  InvertStencilOp,
  KeepStencilOp,
  KeyframeTrack,
  LOD,
  LatheGeometry,
  Layers,
  LessCompare,
  LessDepth,
  LessEqualCompare,
  LessEqualDepth,
  LessEqualStencilFunc,
  LessStencilFunc,
  LevelRoot,
  LevelSystem,
  LevelTag,
  Light,
  LightProbe,
  Line,
  Line3,
  LineBasicMaterial,
  LineCurve,
  LineCurve3,
  LineDashedMaterial,
  LineLoop,
  LineSegments,
  LinearFilter,
  LinearInterpolant,
  LinearMipMapLinearFilter,
  LinearMipMapNearestFilter,
  LinearMipmapLinearFilter,
  LinearMipmapNearestFilter,
  LinearSRGBColorSpace,
  LinearToneMapping,
  LinearTransfer,
  Loader,
  LoaderUtils,
  LoadingManager,
  LocomotionEnvironment,
  LocomotionSystem,
  Locomotor,
  LoopOnce,
  LoopPingPong,
  LoopRepeat,
  MCPRuntime,
  MOUSE,
  Material,
  MaterialBlending,
  MaterialLoader,
  MathUtils,
  Matrix2,
  Matrix3,
  Matrix4,
  MaxEquation,
  Mesh,
  MeshBasicMaterial,
  MeshDepthMaterial,
  MeshDistanceMaterial,
  MeshLambertMaterial,
  MeshMatcapMaterial,
  MeshNormalMaterial,
  MeshPhongMaterial,
  MeshPhysicalMaterial,
  MeshStandardMaterial,
  MeshToonMaterial,
  MinEquation,
  MirroredRepeatWrapping,
  MixOperation,
  MovementMode,
  MultiPointer,
  MultiplyBlending,
  MultiplyOperation,
  NearestFilter,
  NearestMipMapLinearFilter,
  NearestMipMapNearestFilter,
  NearestMipmapLinearFilter,
  NearestMipmapNearestFilter,
  NeutralToneMapping,
  NeverCompare,
  NeverDepth,
  NeverStencilFunc,
  NoBlending,
  NoColorSpace,
  NoNormalPacking,
  NoToneMapping,
  NormalAnimationBlendMode,
  NormalBlending,
  NormalGAPacking,
  NormalRGPacking,
  NotEqualCompare,
  NotEqualDepth,
  NotEqualStencilFunc,
  NullEntity,
  NumberKeyframeTrack,
  Object3D,
  ObjectLoader,
  ObjectSpaceNormalMap,
  OcclusionShadersMode,
  OctahedronGeometry,
  OneFactor,
  OneHandGrabbable,
  OneMinusConstantAlphaFactor,
  OneMinusConstantColorFactor,
  OneMinusDstAlphaFactor,
  OneMinusDstColorFactor,
  OneMinusSrcAlphaFactor,
  OneMinusSrcColorFactor,
  OrthographicCamera,
  PCFShadowMap,
  PCFSoftShadowMap,
  PMREMGenerator,
  PanelDocument,
  PanelUI,
  PanelUISystem,
  Path,
  PerspectiveCamera,
  PhysicsBody,
  PhysicsManipulation,
  PhysicsShape,
  PhysicsShapeType,
  PhysicsState,
  PhysicsSystem,
  Plane,
  PlaneGeometry,
  PlaneHelper,
  PlaybackMode,
  PointLight,
  PointLightHelper,
  Points,
  PointsMaterial,
  PokeInteractable,
  PolarGridHelper,
  PolyhedronGeometry,
  PositionalAudio,
  Pressed,
  PropertyBinding,
  PropertyMixer,
  QuadraticBezierCurve,
  QuadraticBezierCurve3,
  Quaternion,
  QuaternionKeyframeTrack,
  QuaternionLinearInterpolant,
  R11_EAC_Format,
  RED_GREEN_RGTC2_Format,
  RED_RGTC1_Format,
  REVISION,
  RG11_EAC_Format,
  RGBADepthPacking,
  RGBAFormat,
  RGBAIntegerFormat,
  RGBA_ASTC_10x10_Format,
  RGBA_ASTC_10x5_Format,
  RGBA_ASTC_10x6_Format,
  RGBA_ASTC_10x8_Format,
  RGBA_ASTC_12x10_Format,
  RGBA_ASTC_12x12_Format,
  RGBA_ASTC_4x4_Format,
  RGBA_ASTC_5x4_Format,
  RGBA_ASTC_5x5_Format,
  RGBA_ASTC_6x5_Format,
  RGBA_ASTC_6x6_Format,
  RGBA_ASTC_8x5_Format,
  RGBA_ASTC_8x6_Format,
  RGBA_ASTC_8x8_Format,
  RGBA_BPTC_Format,
  RGBA_ETC2_EAC_Format,
  RGBA_PVRTC_2BPPV1_Format,
  RGBA_PVRTC_4BPPV1_Format,
  RGBA_S3TC_DXT1_Format,
  RGBA_S3TC_DXT3_Format,
  RGBA_S3TC_DXT5_Format,
  RGBDepthPacking,
  RGBFormat,
  RGBIntegerFormat,
  RGB_BPTC_SIGNED_Format,
  RGB_BPTC_UNSIGNED_Format,
  RGB_ETC1_Format,
  RGB_ETC2_Format,
  RGB_PVRTC_2BPPV1_Format,
  RGB_PVRTC_4BPPV1_Format,
  RGB_S3TC_DXT1_Format,
  RGDepthPacking,
  RGFormat,
  RGIntegerFormat,
  RawShaderMaterial,
  Ray,
  RayDisplayMode,
  RayInteractable,
  RayPointer,
  RaycastSpace,
  Raycaster,
  RectAreaLight,
  RedFormat,
  RedIntegerFormat,
  ReferenceSpaceType,
  ReinhardToneMapping,
  RenderTarget,
  RenderTarget3D,
  RepeatWrapping,
  ReplaceStencilOp,
  ReverseSubtractEquation,
  RingGeometry,
  SIGNED_R11_EAC_Format,
  SIGNED_RED_GREEN_RGTC2_Format,
  SIGNED_RED_RGTC1_Format,
  SIGNED_RG11_EAC_Format,
  SRGBColorSpace,
  SRGBTransfer,
  Scene,
  SceneUnderstandingSystem,
  ScreenSpace,
  ScreenSpaceUISystem,
  SessionMode,
  ShaderChunk,
  ShaderLib,
  ShaderMaterial,
  ShadowMaterial,
  Shape,
  ShapeGeometry,
  ShapePath,
  ShapeUtils,
  ShortType,
  Skeleton,
  SkeletonHelper,
  SkinnedMesh,
  SlideSystem,
  Source,
  Sphere,
  SphereGeometry,
  Spherical,
  SphericalHarmonics3,
  SplineCurve,
  SpotLight,
  SpotLightHelper,
  Sprite,
  SpriteMaterial,
  SrcAlphaFactor,
  SrcAlphaSaturateFactor,
  SrcColorFactor,
  StatefulBrowserGamepad,
  StatefulButtonAxesDevice,
  StatefulGamepad,
  StatefulKeyboard,
  StaticCopyUsage,
  StaticDrawUsage,
  StaticReadUsage,
  StereoCamera,
  StreamCopyUsage,
  StreamDrawUsage,
  StreamReadUsage,
  StringKeyframeTrack,
  SubtractEquation,
  SubtractiveBlending,
  SyncedEuler,
  SyncedQuaternion,
  SyncedVector3,
  TOUCH,
  TOUCH_DEFAULTS,
  TangentSpaceNormalMap,
  TeleportSystem,
  TetrahedronGeometry,
  Texture,
  TextureLoader,
  TextureUtils,
  Timer,
  TimestampQuery,
  TorusGeometry,
  TorusKnotGeometry,
  TouchPointer,
  Transform,
  TransformSystem,
  Triangle,
  TriangleFanDrawMode,
  TriangleStripDrawMode,
  TrianglesDrawMode,
  TubeGeometry,
  TurnSystem,
  TurningMethod,
  TwoHandsGrabbable,
  Types,
  dist_exports as UIKit,
  UIKitDocument,
  UVMapping,
  Uint16BufferAttribute,
  Uint32BufferAttribute,
  Uint8BufferAttribute,
  Uint8ClampedBufferAttribute,
  Uniform,
  UniformsGroup,
  UniformsLib,
  UniformsUtils,
  UnsignedByteType,
  UnsignedInt101111Type,
  UnsignedInt248Type,
  UnsignedInt5999Type,
  UnsignedIntType,
  UnsignedShort4444Type,
  UnsignedShort5551Type,
  UnsignedShortType,
  VERSION2 as VERSION,
  VSMShadowMap,
  Vector2,
  Vector3,
  Vector4,
  VectorKeyframeTrack,
  VideoFrameTexture,
  VideoTexture,
  Visibility,
  VisibilityState,
  VisibilitySystem,
  WebGL3DRenderTarget,
  WebGLArrayRenderTarget,
  WebGLCoordinateSystem,
  WebGLCubeRenderTarget,
  WebGLRenderTarget,
  WebGLRenderer,
  WebGLUtils,
  WebGPUCoordinateSystem,
  WebXRController,
  WireframeGeometry,
  World,
  WrapAroundEnding,
  XRAnchor,
  XRControllerVisualAdapter,
  XRCylinderLayer,
  XRHandVisualAdapter,
  XRInputDeviceType,
  XRInputManager,
  XRInputVisualAdapter,
  XRLayerState,
  XRLayerSystem,
  XRMesh,
  XROrigin,
  XRPlane,
  XRQuadLayer,
  ZeroCurvatureEnding,
  ZeroFactor,
  ZeroSlopeEnding,
  ZeroStencilOp,
  buildSessionInit,
  createCanvasElement,
  createComponent,
  createSystem,
  defaultHandLayout,
  eq,
  error,
  fetchJsonFile,
  fetchProfile,
  fetchProfileSync,
  fetchProfilesList,
  ge,
  getConsoleFunction,
  getRequiredInputProvider,
  gt,
  initializeWorld,
  isin,
  launchXR,
  le,
  loadInputProfile,
  log,
  lt,
  ne,
  nin,
  normalizeCanvasPointerEventsOptions,
  normalizeReferenceSpec,
  outlineMaterial,
  resolveReferenceSpaceType,
  sampleParabolicCurve,
  setConsoleFunction,
  setWorldPosition,
  setWorldQuaternion,
  stencilMaterial,
  warn,
  warnOnce
};
//# sourceMappingURL=@iwsdk_core.js.map
